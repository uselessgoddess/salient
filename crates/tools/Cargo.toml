[package]
name = "salient-tools"
description = "Determinism checks that are commands rather than manual procedures."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
salient-sim.workspace = true

[lints]
workspace = true
